# TODO Process to get this to work
  # * imports for files and being able to read the files
  # * read the files
  # * find where the CSS is in the files
  # * go through each CSS property to discover if it needs to be changed to utilize DDD
  # * Print the results in the terminal with the line number, what property needs to be changed, and recommendations to change it to
    # * Ensure anything that cannot be done with DDD is ignored.